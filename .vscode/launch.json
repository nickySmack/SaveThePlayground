{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "msedge",
        "request": "launch",
        "name": "Next.js",
        "url": "http://localhost:3000", // Update the URL to match your Next.js app's development URL
        "webRoot": "${workspaceFolder}"
      }
    ]
  }
  